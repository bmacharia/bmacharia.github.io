

Kernel - the process running that interact with the hardware, and getting permissions to go into memory allocate memory etc..

Kernel - is the brain, which version of the kernel that I am using

Kernel arguments - affect the way the system boots

modules - add or remove components without breaking things

mountpoints - map devices and the places that they exist in the filesystem


grep is a datafilter

```bash
# the -i means ignore lowercase and uppercase
grep -i 
```

different partitions on the same block device and are located at different locations of the filesystem

the `find` will find things in the system
